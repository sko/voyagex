- max_height = 100.0
- max_width = 300
-# scroll_width = 0
-# max_width = 0
- @uploads.each do |upload|
  -# geometry = Paperclip::Geometry.from_file(upload.file)
  -# scale = max_height.to_f / geometry.height
  -# width = (geometry.width * scale + 0.49).round
  -# max_width = width if width > max_width
  -# height = (geometry.height* scale + 0.49).round
  -# scroll_width += width
  -#%span.swiper-slide{data: {id: upload.id, address: upload.location.address}}
  %span.swiper-slide{onclick: 'photoClicked({clickedSlide: this})', data: {id: upload.id, address: upload.poi_note.poi.location.address}}
    = image_tag upload.file.url, style: "height:#{max_height.to_i}px;"
-# swiper_style = "width: #{max_width}px; height: #{max_height.to_i}px;"
